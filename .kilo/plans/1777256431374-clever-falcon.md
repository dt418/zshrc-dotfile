# Plan: Add fix-pyenv.sh to Required Tools List

## Objective
Add `fix-pyenv.sh` to the project's required tools / installation checklist so it is properly documented and shown in the doctor checks.

## Current State
- `fix-pyenv.sh` exists at `zsh/config/fix-pyenv.sh` (untracked)
- It is sourced from `zsh/zshrc` (line 16) and aliased as `fixpyenv` in `aliases.zsh` (with `ZSH_CONFIG` fallback)
- It runs automatically on every shell startup to fix pyenv shim/lock issues
- All tests pass, lint is clean

## Changes to Implement

### 1. Update `install.sh` — Doctor/check tools section
Add `fixpyenv` to the CLI tools list so `./install.sh --doctor` checks for it.

**File: `install.sh`**
- Line ~49: Add `"fixpyenv"` to `TOOL_NAMES`
- Line ~50: Add `"Pyenv shim fix"` to `TOOL_DESCS`
- Line ~111: Add check entry to `TOOL_CHECK` array

The check verifies the alias is available via `command -v` (since it's sourced in zshrc) or falls back to checking the script file existence with proper `ZSH_CONFIG` handling.

### 2. Update `README.md` — Required tools section
Add `fixpyenv` to the CLI Tools table.

**File: `README.md`**
- Line ~75-84: Add `"fixpyenv"` row to the tools table with description "Tự động sửa lỗi shim/lock pyenv"
- Add a documentation section for `fixpyenv` explaining its purpose, usage, and how to invoke it manually

## Verification Steps
1. `make lint` — all Zsh syntax valid
2. `make test` — smoke tests pass
3. `./install.sh --doctor` — shows fixpyenv as OK/available
4. `./install.sh --tools` — lists fixpyenv in the tools check

## Notes
- The `fixpyenv` "tool" is a Zsh alias pointing to a bash script. The doctor check uses `command -v` which works for aliases when sourced.
- No functional changes to the `fix-pyenv.sh` script itself.
- All changes follow the existing codebase conventions and bilingual documentation style.